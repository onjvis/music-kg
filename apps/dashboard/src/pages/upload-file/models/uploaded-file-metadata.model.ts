export type UploadedFileMetadata = {
  album?: string; // Spotify ID
  albumName?: string;
  artist?: string; // Spotify ID
  artistName?: string;
  date?: string;
  duration?: number;
  isrc?: string;
  title?: string;
  track?: string; // Spotify ID
};
